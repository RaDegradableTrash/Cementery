using UnityEngine;

public class HeartVisualizer : MonoBehaviour
{
    private void OnEnable()
    {
        // Legacy script placeholder. Visual heartbeat now comes from Animator clip playback.
        enabled = false;
    }
}
